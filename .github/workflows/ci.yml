name: GH-Actions-Demo

on:
  push:
    branches:
      - "gh-actions-demo"
  pull_request:
    branches:
      - "gh-actions-demo"

jobs:
  build:
    environment: "Actions Demo"
    runs-on: ubuntu-latest
    steps:
    - uses: earthly/actions/setup-earthly@v1
      with:
        version: v0.7.0
    - uses: actions/checkout@v2
    - name: Put back the git branch into git (Earthly uses it for tagging)
      run: |
        branch=""
        if [ -n "$GITHUB_HEAD_REF" ]; then
          branch="$GITHUB_HEAD_REF"
        else
          branch="${GITHUB_REF##*/}"
        fi
        git checkout -b "$branch" || true
    - name: Docker Login
      run: docker login --username "$DOCKERHUB_USERNAME" --password "$DOCKERHUB_TOKEN"
    - name: Earthly version
      run: earthly --version
    - name: List satellites
      run: earthly sat --org earthly-sa ls
    - name: Select first satellite 
      run: earthly sat --org earthly-sa select evan1 

    - name: Build python
      run: earthly ./examples/polyglot-repo/python_server/+build
