version: "3"
services:
  react:
    platform: linux/amd64
    image: ezeev/earthly-react-example:dev
    ports:
      - 3001:80 
  rust:
    platform: linux/amd64
    image: ezeev/earthly-rust-example:latest   
    ports:
      - 8000:8000
  go:
    platform: linux/amd64 
    image: ezeev/earthly-go-example:latest 
    ports:
      - 8001:8001
